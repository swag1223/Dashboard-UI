const icons = `@font-face {
    font-family: 'icomoon';
    font-weight: normal;
    font-style: normal;
    font-display: block;
    src:  url('@assets/icomoon/icomoon.woff?d1cx4g') format('woff'),
  }
  
  [class^="icon-"], [class*=" icon-"] {
    font-family: 'icomoon';
    font-size: 20px;
    font-style: normal;
    font-weight: normal;
    font-variant: normal;
    text-transform: none;
    line-height: 1;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
  
  .icon-Vector:before {
    content: "\\e900";
  }`;

export default icons;
